import { createBrowserHistory } from "history";

export const history = createBrowserHistory();

//Usado para redirecionamento no projeto fora dos Componentes React
//Comando para instalar a lib: yarn add history@5.3.0